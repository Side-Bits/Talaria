# Repository Layer

**Purpose**: Data access, pure database operations
**Responsibility**: CRUD operations, database queries, data persistence

## Characteristics:

- Only deals with data storage/retrieval
- No business logic
- Database-agnostic interface
- Returns raw data or simple errors
